version: "2"
run:
  timeout: 3m
  tests: true
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - gosec
    - misspell
  settings:
    gosec:
      excludes:
        - G104
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
