[extend]
useDefault = true

[[allowlists]]
description = "Test fixtures and examples"
paths = [
  '''backend/.*_test\.go''',
  '''docs/.*''',
  '''.*\.example'''
]

[[allowlists]]
description = "Makefile variable references (not real secrets)"
regexes = [
  '''\$\([A-Z_]*TOKEN[A-Z_]*\)'''
]
